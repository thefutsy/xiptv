# No trailing slash, so a symlinked node_modules (as a worktree has) is ignored too.
node_modules
dist/
dist-electron/
release/
.env
*.log

# Vendored ffmpeg binaries: ~139 MB each, fetched by scripts/fetch-ffmpeg.mjs
vendor/

.DS_Store
*.local
